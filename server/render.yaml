# Render.com Blueprint
services:
  - type: web
    name: softwarepersona-api
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
